import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yahya.blockfiller',
  appName: 'block-filler-game',
  webDir: 'dist'
};

export default config;
